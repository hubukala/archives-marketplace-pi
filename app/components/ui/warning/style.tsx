import styled from "styled-components";

const WarningWrapper = styled.div`
    display: flex;
    gap: 10px;
    align-items: center;
`;

const Message = styled.p`
    color: red;
`;

export { WarningWrapper, Message };
